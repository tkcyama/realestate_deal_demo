# PropConnect 開発作業報告書

**プロジェクト名**: PropConnect（法人向け不動産取引プラットフォーム）  
**作業期間**: 2026年5月  
**作業者**: Claude Code (claude-sonnet-4-6)  
**リポジトリ**: https://github.com/tkcyama/realestate_deal_demo

---

## 作業日誌

### 2026年5月6日（UAT・バグ修正・機能追加）

#### 環境整備
- プロジェクトフォルダ内のファイルを整理（`docs/`・`prototypes/` サブフォルダに分類）
- 開発サーバー起動確認（`npm run dev`、`http://localhost:3000`）

#### バグ修正

| # | 対象 | 不具合内容 | 対処 |
|---|------|-----------|------|
| B-01 | 会員登録 | `email rate limit exceeded` エラーが英語で表示される | エラーメッセージを日本語化（「メール送信の上限に達しました。しばらく時間をおいてから…」）。Supabase Dashboard でメール確認を無効化して回避することを案内 |
| B-02 | 管理者 | 会員「却下」操作後に管理者自身がログイン不能になるバグ | 原因：管理者自身の行にも却下ボタンが表示されており、自アカウントを suspended に変更できた。① 停止・審査中画面にログアウトボタン追加 ② 会員管理画面で自分自身の行にはボタン非表示（「（自分）」と表示） ③ `LogoutButton` 共通コンポーネント新規作成 |
| B-03 | ダッシュボード | `performance.measure` で負のタイムスタンプエラーが発生 | `dashboard/layout.tsx` および `dashboard/page.tsx` に `export const dynamic = 'force-dynamic'` を追加し、レイアウト・ページ双方の `redirect()` 競合を解消 |
| B-04 | 物件詳細 | 地下階数を登録しても画面上に表示されない | `properties/[id]/page.tsx` の階数表示ロジックが `floors_above` のみ参照していた。地上・地下を `地上X階 / 地下Y階` 形式で両方表示するよう修正 |
| B-05 | 物件フィルター | キーワードフィルター使用時に「uncontrolled FieldControl」警告が発生 | `defaultValue` + `ref` のアンコントロールド実装を `value` + `useState` のコントロールド実装に全面書き換え |
| B-06 | 全画面 | サイドバーが `/offers`・`/properties`・`/notifications` 等で表示されない | Next.js ルートグループ `(protected)` を新設し、全保護ルートを統合。共通レイアウト `(protected)/layout.tsx` でサイドバー・認証・停止画面を一元管理 |
| B-07 | 地図 | 地図表示画面（`/map`）が存在しないためテスト P2-5 が実施不可 | `/map` ページを新規作成（保有物件・公開中物件をピン色分けして表示）。サイドバーに「🗺️ 地図表示」リンクを追加 |

#### 機能改善・追加

| # | 対象 | 内容 |
|---|------|------|
| I-01 | 管理者 | 管理者専用ページへ一般会員がアクセスした際に「管理者専用ページです。アクセス権限がありません。」エラーメッセージをダッシュボード上部に表示 |
| I-02 | 物件登録・編集 | 都道府県フィールドに4文字制限を追加（Zodバリデーション `.max(4)` + Input `maxLength={4}`）。日本最長の都道府県名「神奈川県」「和歌山県」「鹿児島県」が4文字であることに基づく |
| I-03 | 全画面 | サイドバーのロゴ（PropConnect）をクリックするとダッシュボードへ戻るリンクに変更 |
| I-04 | 全画面 | 全保護ページのコンテンツ上部に「🏠 トップへ戻る」スティッキーヘッダーバーを追加（ダッシュボード本体では非表示） |

#### テスト計画書更新
- `テスト計画書_Phase1-3.md` の全テストケーステーブル（18セクション・68件）に「判定」列を追加
- UAT 実施中のため各テストケースへの PASS / FAIL 記入が可能になった

---

## 技術スタック

| 区分 | 採用技術 |
|------|---------|
| フロントエンド | Next.js (TypeScript) + Tailwind CSS + shadcn/ui |
| バックエンド/DB | Supabase (PostgreSQL + Auth + Storage + RLS) |
| 地図 | Google Maps JavaScript API / Geocoding API |
| AI | Claude AI API (claude-sonnet-4-6) ※Phase 4で使用予定 |
| PDF生成 | Puppeteer ※Phase 4で使用予定 |
| メール | Resend API ※Phase 5で使用予定 |
| ホスティング | Vercel + Supabase |

---

## 実施作業概要

### Phase 1：基盤構築・会員管理

#### 1-1. プロジェクトセットアップ
- `create-next-app` による Next.js プロジェクト初期化
- Tailwind CSS・shadcn/ui・各種依存パッケージのインストール
- `.env.local` への Supabase / Google Maps API キー設定

#### 1-2. DBスキーマ設計・デプロイ（全フェーズ分）
- **設計方針**: RLS後付け改修コストを避けるため、全フェーズ分のスキーマを一括設計・先行デプロイ
- `supabase/migrations/0001_initial_schema.sql`（911行）として実装
  - **テーブル数**: 17（profiles, properties, office_details, residential_details, commercial_details, logistics_details, hotel_details, sale_offers, purchase_offers, loan_offers, documents, ai_extraction_jobs, property_images, transactions, transaction_details, notifications, audit_logs）
  - **ENUM数**: 10
  - **インデックス数**: 43
  - **RLSポリシー数**: 54
  - **ヘルパー関数**: 5（`is_approved_member()`, `is_admin_user()`, `has_property_detail_access()` 等）
  - **トリガー数**: 13（新規ユーザー登録時の profiles 自動生成 等）
  - **Storageバケット**: 3（property-images, documents, teasers）

#### 1-3. 認証・会員管理フロー実装
- Supabase Auth (`@supabase/ssr`) を用いたログイン・新規登録ページ
- `src/middleware.ts` によるルート保護（未認証→ /auth/login、管理者専用ルートのガード）
- `handle_new_user()` トリガーによる profiles 自動生成
- 管理者承認フロー（`profiles.status` = pending → approved）
- 審査中・停止ユーザー向けブロック画面（dashboard layout）

#### 1-4. 管理者：会員管理画面
- `/admin/members` — 会員一覧・承認/却下/停止/再有効化ボタン
- `member-actions.tsx` — Supabase update + `router.refresh()` によるリアルタイム反映

---

### Phase 2：物件管理・地図表示（完了）

#### 2-1. Google Maps 地図表示
- `@vis.gl/react-google-maps` を使用
- AdvancedMarker + Pin によるピン色分け
  - 紺（#1F3864）: 自分の保有物件
  - 赤（#C00000）: 公開中物件
  - 緑（#1F6B3A）: 取引事例
- InfoWindow ポップアップ（物件名・住所・用途・価格・NOI利回り）
- 凡例コンポーネント（地図左下）
- Google Geocoding API による住所→緯度経度変換（`src/lib/geocoding.ts`）

#### 2-2. 物件登録フォーム（`/properties/new`）
- react-hook-form + zod によるバリデーション
- **入力セクション構成**:
  - 基本情報（物件名・所在地・用途・建築年・構造・地上/地下階数）
  - 所在地（都道府県・市区町村・町域・番地以降に分割保持）
  - 交通（路線・最寄り駅・徒歩分数）
  - 面積情報（延床・専有・土地）
  - 財務情報（価格・NOI・NCF・月額賃料収入）
  - テナント情報（テナント数・稼働率）
- 自動計算: NOI利回り・NCF利回り・専有坪単価・専有面積（㎡換算）
- 下書き保存 / 公開申請 の2段階ボタン
- バリデーションエラー時に上部バナーで具体的な項目名を表示・自動スクロール

#### 2-3. 物件詳細ページ（`/properties/[id]`）
- 物件情報の表示
- 売主本人向け: 「公開申請」「申請取り消し」ステータス操作ボタン
- エラー表示・更新失敗時のメッセージ

#### 2-4. 管理者：物件承認管理（`/admin/properties`）
- 承認待ち物件一覧（バッジで件数表示）
- 「承認・公開」「差し戻し」ボタン

#### 2-5. 物件編集ページ（`/properties/[id]/edit`）
- 既存データを全フォームフィールドにプリセット（`reset()` で初期値注入）
- 住所フィールド変更時のみ Geocoding API 再実行（コスト節約）
- アクセス制御: 売主本人 かつ `draft` / `pending_approval` のみ編集可能
- 下書き保存 / 公開申請 の2段階ボタン、キャンセルで詳細ページへ戻る

#### 2-6. 物件ステータス管理の拡張（P2-02）
- 公開中（`published`）物件に「非公開に戻す」ボタンを追加（amber系カラー）
- 非公開に戻すと `draft` に遷移し、詳細ページに「編集」ボタンが出現
- 詳細ページの「編集」ボタンを `draft` / `pending_approval` のみ表示に変更（公開中は非表示）

#### 2-7. 価格の億円単位入力・表示（P2-03）
- `format.ts` に `formatPropertyPrice` 関数を追加（常に億円単位、例: 50億円・1.5億円・0.5億円）
- 物件登録・編集フォームの売却希望価格を億円単位入力に変更（`step="0.1"`）
- 保存時に億円→円変換（`× 100,000,000`）、編集ロード時に円→億円変換（`÷ 100,000,000`）
- **利回り計算は保存後の円単位 price・円単位 NOI/NCF で統一**（単位ミス防止）
- NOI・NCF・月額賃料収入はフォーム入力・DB保存ともに円単位のまま

#### 2-8. 階数バリデーション修正（P2-04）
- 地上階数・地下階数の最小値を両方 `0` に統一（旧: 地上 `1` 以上）

#### 2-9. 住所正規化（`src/lib/normalize.ts`）（P2-05）
- `normalizeAddress(s)` 関数を新規作成、物件登録・編集の保存時に全住所フィールドへ適用
- **正規化ルール**:
  1. 全角英数字・記号 → 半角（`３９` → `39`、`－` → `-`）
  2. 漢数字 → 半角数字（単純数字: `三` → `3`、複合数字: `三十九` → `39`、千百十対応）
  3. `番(地)?数字(号)` → ハイフン区切り（`39番7号` / `39番地7号` / `39番地7` → `39-7`）
  4. 末尾の `番地` 除去（`39番地` → `39`）
  5. 連続ハイフンを1つに統一、末尾ハイフン除去
  - ※ `番町`（地名）・`号館`・`号室` 等は誤変換しない設計

#### 2-10. 物件一覧フィルター検索（P2-フィルター）
- `property-filters.tsx`（Client Component）を新規作成
- URLパラメータ（`searchParams`）でフィルター状態を管理
- **フィルター項目**: キーワード（物件名・住所 部分一致）・用途・都道府県・価格帯（億円入力）
- 保有物件・公開中物件の両セクションに同一フィルターを適用
- `<Suspense>` ラッパーで `useSearchParams` の Next.js 要件に対応

#### 2-11. ドキュメント整備

- `CLAUDE.md` に物件管理仕様詳細セクションを追加
  - 物件ステータス遷移図・価格単位仕様・住所正規化ルール表・フィルター仕様
- `不動産取引プラットフォーム_要件定義書.docx` に追記（python-docx を使用）
  - 3.2.4 物件ステータス管理（新規）
  - 3.2.5 価格・金額の入力・表示単位（新規）
  - 3.2.6 住所正規化ルール（新規）
  - Phase 2 成果物リストに4件追記
- `不動産取引プラットフォーム_要件定義書.md` を新規作成（docx の完全 Markdown 変換版、624行）
  - 全テーブル・箇条書き・フェーズ計画・テスト計画を Markdown 形式に変換
  - 以降はこの `.md` ファイルを正として管理

---

## 主要ファイル構成

```
src/
├── app/
│   ├── auth/login/           # ログインページ
│   ├── auth/register/        # 新規登録ページ
│   ├── dashboard/            # ダッシュボード（地図）
│   ├── properties/
│   │   ├── page.tsx          # 物件一覧（フィルター検索付き）
│   │   ├── property-filters.tsx  # フィルターUIコンポーネント（Client）
│   │   ├── new/              # 物件登録フォーム
│   │   └── [id]/
│   │       ├── page.tsx      # 物件詳細 + ステータス操作
│   │       ├── property-status-actions.tsx  # ステータス変更ボタン群
│   │       └── edit/         # 物件編集フォーム（新規）
│   ├── admin/
│   │   ├── members/          # 会員承認管理
│   │   └── properties/       # 物件承認管理
│   └── profile/              # プロフィール
├── components/
│   ├── layout/sidebar.tsx    # サイドバーナビゲーション
│   ├── map/property-map.tsx  # Google Maps コンポーネント
│   └── ui/                   # shadcn/ui コンポーネント群
├── lib/
│   ├── supabase/client.ts    # ブラウザ用 Supabase クライアント
│   ├── supabase/server.ts    # サーバー用 Supabase クライアント
│   ├── geocoding.ts          # Google Geocoding API
│   ├── format.ts             # 価格・利回り・面積フォーマッター
│   └── normalize.ts          # 住所正規化ユーティリティ（新規）
└── types/index.ts            # 型定義（Profile, Property, Offer等）
supabase/
└── migrations/
    └── 0001_initial_schema.sql
```

---

## 解決した主な技術的問題

| 問題 | 原因 | 解決策 |
|------|------|--------|
| `useSearchParams()` ビルドエラー | Next.js 16 で Suspense 必須 | ログイン・プロフィールページに `<Suspense>` ラッパー追加 |
| zod + react-hook-form 型エラー | `z.coerce.number()` が `unknown` を返す | 全数値フィールドを `z.string().refine()` に統一 |
| Supabase `.select()` API エラー | 第2引数に `count` オプション不可 | `.select('id')` のみに変更 |
| 公開申請ボタン無反応 | バリデーションエラーがコンソールのみ出力 | 上部バナーに具体的なエラー項目を一覧表示・自動スクロール |
| 地下階数バリデーションエラー | フォームにUI入力欄なく `undefined` が渡る | 地下階数の入力欄を追加（地上/構造/建築年と同列に配置） |
| 管理者の初回ログイン | 審査中のため管理画面にアクセス不可 | Supabase SQL Editor で直接 `is_admin = true` に更新 |
| フィルター関数の TypeScript 型エラー | Supabase クエリビルダーのジェネリクス型に対応する型注釈が複雑 | `applyFilters` 関数の引数を `any` 型で定義し回避 |

---

### Phase 3：オファー・取引フロー（完了）

#### 3-1. 型定義追加（`src/types/index.ts`）
- `SaleOffer`・`PurchaseOffer`・`Notification`・`NotificationType` 型を追加
- `SALE_OFFER_STATUS_LABELS`・`PURCHASE_OFFER_STATUS_LABELS` ラベルマップを追加

#### 3-2. Server Actions（`src/app/offers/actions.ts`）
- `sendSaleOffer` — 売主 → 買主への売却オファー送信（重複チェック・通知作成付き）
- `respondSaleOffer` — 買主による「検討する」「見送り」返答
- `sendPurchaseOffer` — 買主 → 売主への購入オファー送信（希望価格・条件・メッセージ）
- `respondPurchaseOffer` — 売主による「承諾」「条件変更」「拒否」返答
- `agreeOffer` — 買主による取引合意確定（`accepted` → `agreed` 遷移）
- `markNotificationRead` / `markAllNotificationsRead` — 通知既読管理

#### 3-3. 売却オファー送信ページ（`/offers/sale/new`）
- 物件ID（`?propertyId=xxx`）から対象物件を特定（売主本人・公開中のみ）
- 承認済み会員リストからラジオボタンで送信先を選択
- メッセージ入力・送信後はオファー一覧へリダイレクト

#### 3-4. 購入オファー送信ページ（`/offers/purchase/new`）
- `saleOfferId`（検討中オファーID）・`propertyId` をクエリパラメータで受け取り
- 希望価格を億円単位で入力（円換算プレビュー表示）
- 購入条件・メッセージを入力して送信

#### 3-5. オファー一覧マイページ（`/offers`）
- 4セクション構成（受信売却オファー / 送信売却オファー / 受信購入オファー / 送信購入オファー）
- 未返答件数バッジ表示
- ステータス別カラーバッジ
- `SaleOfferActions`（検討する/見送りボタン）
- `PurchaseOfferActions`（承諾/条件変更/拒否、条件変更モードでは価格・条件入力欄を展開）
- `AgreeButton`（承諾後の合意確定ボタン）
- 条件変更提案の表示（counter_price / counter_conditions）

#### 3-6. 通知センター（`/notifications`）
- 全通知の一覧表示（通知タイプ別アイコン）
- 個別既読・一括既読ボタン
- サイドバーに「通知」ナビ項目を追加

#### 3-7. 物件詳細ページ権限制御強化（`/properties/[id]`）
- `sale_offer.status = 'considering'` の場合のみ収支詳細を開放（RLS連動）
- 売主向け：「売却オファーを送信する」ボタン追加（`/offers/sale/new?propertyId=xxx`）
- 買主向け（返答待ち）：「オファーを確認・返答する」リンク表示
- 買主向け（検討中）：「購入オファーを送る」ボタン表示（`/offers/purchase/new?...`）

---

## 残作業（未実装フェーズ）

| フェーズ | 内容 | 状態 |
|---------|------|------|
| Phase 2 | 物件管理・地図表示・フィルター検索・編集・ステータス管理 | **完了** |
| Phase 3 | オファー管理（送受信・ステータス管理・RLS連動） | **完了** |
| Phase 4 | AI自動処理・Teaser生成（PDF/Excel/Word解析・Puppeteer）＋P2-01（物件資料からの自動入力） | 未着手 |
| Phase 5 | レンダー機能・取引完了フロー | 未着手 |
| Phase 6 | 取引事例DB・検索・統計分析 | 未着手 |

---

## 改善課題（動作確認フィードバック）

### Phase 2 課題

| # | 対象 | 内容 | 優先度 |
|---|------|------|--------|
| P2-01 | 物件登録フォーム | PDF・登記簿謄本からの自動入力対応。物件資料（PDF/Word/Excel）をアップロードするとAIが各項目を自動抽出してフォームにプリセットする。※Phase 4のAI解析基盤と連動 | 高 |
| P2-02 | ~~物件ステータス管理~~ | ~~公開中物件を非公開（draft）に戻す「非公開に戻す」ボタン追加。非公開後は編集ボタンが表示されるよう詳細ページも修正~~ **完了** | 高 |
| P2-03 | ~~価格表示~~ | ~~`formatPropertyPrice` 関数を追加し物件一覧・詳細の売却希望価格を常に億円単位表示~~ **完了** | 中 |
| P2-04 | ~~階数バリデーション~~ | ~~地上・地下階数の最小値を 0 に統一~~ **完了** | 低 |
| P2-05 | ~~住所正規化~~ | ~~`normalizeAddress` 関数を作成。全角数字・漢数字（千百十含む複合数字）→ 半角変換を登録・編集保存時に適用~~ **完了** | 中 |

---

## Supabase 手動作業ログ（SQL Editorで実施）

```sql
-- 管理者アカウント設定
UPDATE profiles SET status = 'approved', is_admin = true
WHERE email = 'takeshi2013260@gmail.com';

-- 住所分割・交通情報カラム追加
ALTER TABLE properties
  ADD COLUMN IF NOT EXISTS prefecture      text,
  ADD COLUMN IF NOT EXISTS city            text,
  ADD COLUMN IF NOT EXISTS town            text,
  ADD COLUMN IF NOT EXISTS address_detail  text,
  ADD COLUMN IF NOT EXISTS nearest_line    text,
  ADD COLUMN IF NOT EXISTS nearest_station text,
  ADD COLUMN IF NOT EXISTS walk_minutes    integer;
```
