# PropConnect — 不動産取引プラットフォーム

## プロジェクト概要

法人向け不動産取引プラットフォーム。不動産ファンド・レンダーを対象に、物件情報管理・オファー取引・AI解析・取引事例DBを一元管理する。

## 技術スタック

- **フロントエンド**: Next.js (TypeScript) + Tailwind CSS + shadcn/ui
- **バックエンド/DB**: Supabase (PostgreSQL + Auth + Storage + RLS)
- **地図**: Google Maps JavaScript API（Geocoding API含む）
- **AI**: Claude AI API（claude-sonnet-4-6）
- **PDF生成**: Puppeteer（HTML → PDF）
- **メール通知**: Resend API
- **ホスティング**: Vercel + Supabase
- **CI/CD**: GitHub Actions + Vercel

## 対象ユーザーとロール

1つの会員アカウントが複数ロールを同時保持可能（案件ごとに切り替わる）。

| ロール | 説明 |
|--------|------|
| 売主 | 物件を登録・公開し、買主にオファーを送る |
| 買主 | 物件を閲覧し、購入オファーを送る |
| レンダー | 取引合意後にローンオファーを受け取る |
| 管理者 | 会員申請の承認・物件公開の最終承認 |

## 実装フェーズ計画

| フェーズ | 内容 | 想定期間 |
|---------|------|---------|
| Phase 1 | 基盤構築・会員管理（Supabase Auth・管理者承認フロー） | 6〜8週 |
| Phase 2 | 物件管理・地図表示（Google Maps・物件CRUD・フィルター） | 5〜7週 |
| Phase 3 | オファー・取引フロー（送受信・ステータス管理・RLS連動） | 6〜8週 |
| Phase 4 | AI自動処理・Teaser生成（PDF/Excel/Word解析・Puppeteer） | 6〜8週 |
| Phase 5 | レンダー機能・取引完了フロー | 4〜5週 |
| Phase 6 | 取引事例DB・検索・統計分析 | 4〜5週 |

## 実装方針（決定事項）

- **DBスキーマを全フェーズ分まとめて先に設計・Supabaseに入れる**（RLSを後付けすると改修コストが大きいため）
- Phase 1（会員管理）→ Phase 2（地図・物件）の順に進める
- `realestate_deal_demo/index.html` のモックUIを Next.js コンポーネントへ移植しながら実装する

## 既存資産

| ファイル | 内容 |
|--------|------|
| `index.html` | 全画面を網羅したUIモック（ログイン・地図・物件一覧・オファー・AI解析・取引事例） |
| `../PropConnect_Demo.html` | 同内容のコピー |
| `../不動産取引プラットフォーム_要件定義書.docx` | 要件定義・実装計画・テスト計画（フルバージョン） |

## 重要設計ポイント

### セキュリティ（最優先）
- Supabase RLS でロール・オファーステータスに応じたアクセス制御を厳格に実装
- 物件詳細情報はオファー受理後のみ閲覧可能
- 取引事例はテナント名・個人情報を匿名化して公開

### 地図ピンの色分け
| 種別 | 色 |
|------|-----|
| 保有物件 | 紺（#1F3864） |
| 売却物件 | 赤（#C00000） |
| 取引事例 | 緑（#1F6B3A） |

### AI処理（Phase 4）
- Claude API で PDF/Excel/Word からNOI・NCF・RR等を自動抽出
- 抽出結果は確認画面を経てDB登録（自動確定しない）
- Teaser生成は30秒以内を目標
- 精度目標：主要財務項目（NOI・NCF）で95%以上

## 認証フロー
1. Web申請フォーム送信
2. 確認メール送信
3. 管理者が審査・承認/却下
4. 承認後にパスワード設定メール送信
5. 初回ログイン時にロール選択（複数可）

## 用途別 追加情報項目（要件定義書 追補）

### オフィス（office）
| 項目 | 備考 |
|------|------|
| 平均賃料（月額・坪単価） | 全テナントの加重平均 |
| 最高賃料（坪単価） | 館内テナント中の最高値 |
| 最低賃料（坪単価） | 館内テナント中の最低値 |
| 直近成約賃料（坪単価） | 最新の新規契約・更新時の賃料 |
| 普通借割合 | 全テナント数に占める普通借の割合（%） |
| 専有面積合計（m²・坪） | |
| 専有坪単価 | 売却希望価格 ÷ 専有面積（坪） |

### レジデンシャル（residential）
| 項目 | 備考 |
|------|------|
| 総Unit数 | |
| 間取り内訳 | 例：2LDK×10戸、3LDK×5戸（JSONB で保持） |
| 平均賃料（月額グロス・坪単価） | 両方保持 |
| 最高賃料（月額グロス） | |
| 最低賃料（月額グロス） | |
| 直近成約賃料（月額グロス・坪単価） | |
| 普通借割合 | 全Unit数に占める普通借の割合（%） |
| 専有面積合計（m²・坪） | |
| 専有坪単価 | 売却希望価格 ÷ 専有面積（坪） |

### ホテル（hotel）
| 項目 | 備考 |
|------|------|
| 総客室数 | |
| 客室タイプ内訳 | 例：シングル×30室、ツイン×50室（JSONB で保持） |
| 平均客室面積（m²） | |
| オペレーター名 | |
| 契約形態 | 賃貸借・MC（管理委託）・フランチャイズ等 |
| ADR（平均客室単価、円） | 直近12か月 |
| Occ（稼働率、%） | 直近12か月 |
| RevPAR（円） | ADR × Occ |
| GOP（円・GOP率%） | 直近12か月 |
| PerKey（円） | 売却希望価格 ÷ 総客室数 |

### 取引事例（transactions）追加項目
| 項目 | 備考 |
|------|------|
| 平均賃料（坪単価） | 成約時点の平均賃料 |
| 専有坪単価 | 成約価格 ÷ 専有面積（坪） |
| PerKey | ホテル物件のみ（成約価格 ÷ 客室数） |
| 売主属性 | `fund` / `reit` / `corporate` / `individual` / `other` |
| 買主属性 | 同上 |
